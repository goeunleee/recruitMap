# prompts

커밋 1개 = 섹션 1개.

## 섹션 형식

```
## (n) stage
- 프롬프트:
- 출력요지:
- 리뷰검증:
```

## (1) docs
- 프롬프트: 채용 파이프라인 보드를 만든다. 단계는 서류검토 / 면접 / 처우협의 / 최종(합격·불합격). 드래그 앤 드롭은 빼고 키보드로 단계 이동·상세 열람. 기능 하나당 커밋 하나. 모든 작업은 prompts.md에 남기고 커밋과 1:1. 못 끝낸 기능은 decisions.md에 무엇을 왜 남겼는지 적는다. 섹션 형식은 stage > 프롬프트 > 출력요지 > 리뷰검증.
- 출력요지: prompts.md, decisions.md를 추가하고 섹션 템플릿을 고정했다. 드래그 앤 드롭 제외는 decisions.md에 남겼다.
- 리뷰검증: 보드 코드 없이 기록 파일만 추가됨. 이후 커밋마다 이 파일에 섹션이 하나씩 붙어야 함.

## (2) types
- 프롬프트: 파이프라인 단계 상수와 지원자 타입을 추가한다. 지원자는 id, 이름, 직무, 지원일, 현재 단계, 최종 결과(합격/불합격/없음), version을 가진다.
- 출력요지: `src/api/stages.ts`에 4단계와 최종합격/불합격 라벨을 넣었다. `src/api/types.ts`에 Applicant 타입을 넣었다.
- 리뷰검증: 컬럼은 서류검토/면접/처우협의/최종. 최종 결과는 단계와 분리된 필드라 같은 최종 컬럼에서 합격/불합격을 구분할 수 있다.

## (3) board
- 프롬프트: 지원자 카드를 스텝별로 화면에 그린다. 상단에 검색 필드를 추가한다. 단순 텍스트필드만. 필터는 API 연결할 때 고려한다.
- 출력요지: 4컬럼 보드와 카드(이름, 직무, 지원일, 현재 단계)를 넣었다. 최종 컬럼은 합격/불합격을 표시한다. 화면용 고정 목 데이터를 썼다. 보드 상단에 검색 텍스트 인풋만 넣었다.
- 리뷰검증: 단계 이동, persist, 상세는 넣지 않음. 검색 입력값이 목록을 걸러내지 않는다.

## (4) mock-api
- 프롬프트: mock API를 구성한다. 보드 조회에만 우선 API를 연결한다.
- 출력요지: `src/api/applicants.ts`에 list/get/updateStage를 넣었다. localStorage persist, 시드 200건, 호출마다 200–800ms 지연, 약 15% 실패. 실패 시 저장하지 않는다. 보드 목록은 `listApplicants`로 불러오고 로딩/실패 시 다시 시도를 넣었다. 고정 목 데이터는 제거했다.
- 리뷰검증: 단계 이동 API, 검색 필터, Zustand는 연결하지 않음.

## (5) search-filter
- 프롬프트: 이름 검색과 직무별 필터를 추가한다. 직무는 검색 필드 하단, border-radius 50% 칩, 기본 outlined, 선택 시 contained, 중복 선택 가능.
- 출력요지: 이름 입력으로 카드를 거르고, 직무 칩 다중 선택 필터를 넣었다. 목록은 이미 불러온 데이터에서 필터한다.
- 리뷰검증: 선택된 칩이 잘 보이게 기존 배경색·테두리색을 반전해 흰 배경 검은 텍스트로 수정함.

## (6) stage-move
- 프롬프트: 각 지원자마다 상태를 변경할 수 있게 한다. 카드 하단에 좌우 화살표 액션 버튼을 넣고 클릭하면 단계가 바뀐다. 카드가 탭된 상태에서 방향키로도 단계 변경이 가능해야 한다.
- 출력요지: 카드 하단에 이전/다음 단계 버튼을 넣었다. 카드에 포커스가 있을 때 왼쪽/오른쪽 방향키로 단계를 바꾼다. 이동은 updateApplicantStage를 호출한다.
- 리뷰검증: 이동한 카드는 다음/이전 스텝의 맨 첫 카드로 가게 함. 방향키 버튼에 탭된 뒤 엔터로만 이벤트가 발생하게 함. 최종으로 갈 때 방향키 옆 드롭다운에서 합격/불합격을 고른 뒤에만 넘어가게 함. 드롭다운 빈 상태는 플레이스홀더 '결과', 색상 text.disabled.

## (7) detail-panel
- 프롬프트: 지원자 상세를 오른쪽 사이드패널(RNB)로 구현한다.
- 출력요지: 카드 정보 영역을 누르면 오른쪽에 상세 패널이 열린다. 이름, 직무, 지원일, 단계를 보여 주고 닫기 버튼과 Escape로 닫는다.
- 리뷰검증: 상세에서 생년월일·성별이 지원자마다 다르게 보이게 함. RNB 외부 클릭하면 닫히게 함.

## (8) undo
- 프롬프트: 제일 최근에 단계를 바꾼 카드에만 Undo 버튼을 넣는다.
- 출력요지: 마지막 단계 이동 카드에만 Undo 버튼을 넣었다. 누르면 직전 단계로 되돌리고 버튼은 사라진다. 이동이 실패하면 버튼을 보여 주지 않는다.
- 리뷰검증:

## (9) zustand
- 프롬프트: 단계 이동 상태를 Zustand로 다룬다. persist는 목 API(localStorage)를 유지한다.
- 출력요지: 목록·단계 이동·Undo를 `usePipelineStore`로 옮겼다. 낙관적 업데이트와 실패 시 원상복구는 그대로다. 검색·상세 선택은 보드 로컬 상태로 둔다.
- 리뷰검증: 검색 필드를 스토어로 관리하고 새로고침해도 유지되게 함.

## (10) empty-state
- 프롬프트: 로딩/에러 외에 빈 상태를 처리한다.
- 출력요지: 검색·필터 결과가 없으면 검색 결과가 없습니다를 보여 준다. 데이터가 없으면 지원자가 없습니다를 보여 준다. 단계 컬럼이 비면 컬럼 안에 지원자가 없습니다를 넣는다.
- 리뷰검증: 검색이나 필터가 하나라도 있으면 지원자가 없습니다 대신 검색 결과가 없습니다로 함. 전체 빈값도 같게 함.

## (11) conflict
- 프롬프트: 같은 카드를 여러 번 이동해도 데이터가 깨지지 않게 version 충돌을 처리한다.
- 출력요지: 단계 이동 API가 expected version과 다르면 저장하지 않는다. 충돌 시 목록을 다시 읽고 피드백을 보여 준다.
- 리뷰검증:
